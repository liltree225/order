server.port: 5123

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:liquibase/db.changelog-master.yaml
    enabled: true
